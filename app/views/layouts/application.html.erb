<!DOCTYPE html>
<html>
<head>
  <title></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">

  <link href="https://fonts.googleapis.com/css2?family=Yuji+Mai&family=Zen+Old+Mincho:wght@700&display=swap" rel="stylesheet">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
</head>
<body class="bg-stone-100 font-serif flex flex-col min-h-screen p-0 m-0">
  <%= render 'layouts/header_logged_in' if user_signed_in? %>
  <%= render 'layouts/header_logged_out' unless user_signed_in? %>

  <div class="container mx-auto my-4">
    <% if notice.present? %>
      <p class="notice text-green-700 px-2 py-2 rounded relative" role="alert">
        <%= notice %>
      </p>
    <% end %>
    <% if alert.present? %>
      <p class="alert bg-red-100 border border-red-400 text-red-700 px-2 py-3 rounded relative" role="alert">
        <%= alert %>
      </p>
    <% end %>
  </div>

  <main class="flex-grow">
    <%= yield %>
  </main>

  <%= render 'layouts/footer' %>
</body>
</html>
